/** A run has three chapters, created together with it (§3.2); matches `chapters_number_range`. */
export const CHAPTER_NUMBERS: readonly number[] = Object.freeze([1, 2, 3])

/** Starting values and default households belong to the first chapter (§4.2). */
export const FIRST_CHAPTER = 1
