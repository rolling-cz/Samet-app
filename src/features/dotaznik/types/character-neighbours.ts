/** Where „Předchozí", „Další" and „Další nevyplněná" lead, as registry IDs; absent at either end of the panel. */
export interface CharacterNeighbours {
  previous?: string
  next?: string
  nextUnfilled?: string
}
