/** `RANDOM(n)` compares a roll against a percentage, so the die has a hundred sides (§7.4). */
export const DICE_SIDES = 100

/** Lowest face; with `DICE_SIDES` it gives the 1–100 range the engine expects. */
export const DICE_MIN = 1
